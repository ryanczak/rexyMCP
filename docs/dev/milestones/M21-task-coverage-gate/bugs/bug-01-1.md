# Bug 1 on phase-01: Format gate failure — rustfmt diff in mod.rs:141

**Severity:** blocker
**Status:** open
**Filed:** 2026-06-16

## What's wrong

`cargo fmt --all --check` exits non-zero with a diff in
`executor/src/agent/mod.rs` at line 141:

```
-    let mut task_states: HashMap<String, crate::store::sessions::event::TaskState> = seeded
-        .iter()
-        .map(|t| (t.id.clone(), t.state))
-        .collect();
+    let mut task_states: HashMap<String, crate::store::sessions::event::TaskState> =
+        seeded.iter().map(|t| (t.id.clone(), t.state)).collect();
```

The code is correct; rustfmt wants the chain collapsed onto one line after a
line break at the `=`.

## What should happen

`cargo fmt --all --check` exits 0. The format gate is a hard DoD requirement.

## How to fix

Run `rustfmt executor/src/agent/mod.rs` (the form prescribed in CLAUDE.md for
fixing format issues in files the phase touched). Verify `cargo fmt --all
--check` exits 0 afterward. No logic change required.

## Verification

- [ ] `cargo fmt --all --check` exits 0 with no diff output
- [ ] `cargo test` still reports 814 passed
