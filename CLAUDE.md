# CLAUDE.md

This repository uses the rexyMCP architect/executor workflow. The full project
contract — read-first docs, commands, hard rules, testing and commit conventions —
lives in `REXYMCP.md`, imported below so it loads into context automatically.

@REXYMCP.md
